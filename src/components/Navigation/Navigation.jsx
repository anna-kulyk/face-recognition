import React from 'react';
import './Navigation.css';

const Navigation = () => {
    return (
        <nav className='navigation'>
            <a href="/">Sign Out</a>
        </nav>
    );
};

export default Navigation;